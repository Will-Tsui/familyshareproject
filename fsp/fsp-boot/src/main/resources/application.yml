server:
  port: 8080

#DataSource
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devbase?serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver


mybatis:
  mapper-locations: classpath:mapping/*.xml  #注意：一定要对应mapper映射xml文件的所在路径
  type-aliases-package: com.tsui.fsp.vo  # 注意：对应实体类的路径